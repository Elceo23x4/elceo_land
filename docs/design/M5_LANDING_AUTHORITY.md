# Landing authority revision — 2026-09-25

The eight unchanged PNGs under `references/m5-landing/` are the current landing composition authority, in section01–08 filename order. Their exact uploaded identity and SHA-256 are recorded in `M5_LANDING_ASSET_PREFLIGHT.json` and verified by `check-m5-assets.mjs`.

Explicit user corrections supersede conflicting older scene/editorial instructions only:

- Scene2 uses CSS burgundy/black only; no scenic background.
- Scene4 preserves image styling but uses end-to-start vertical placement, as explicitly confirmed after preflight. No numbering/lower strokes.
- Scene5 uses independent environment, panels and foreground observer.
- Scene6 uses the approved object mosaic, not dashboard imagery.
- Scene7 uses the orange field and “Continue with Google”, through existing canonical signup/auth ownership.
- Footer is the eighth independent full-width scene.

The historical `approved-landing-reference.png` remains unchanged and integrity-protected. It is not deleted or silently overwritten. None of these visual changes supersedes frozen backend, auth/session, dashboard, security or resource authorities.
