/**
 * Geometry Calibration Overlay — DORMANT
 * This overlay is disabled in runtime (SHOW_GEOMETRY_CALIBRATION = false).
 * Kept as a stub for build compatibility.
 */

interface Props { showLabels?: boolean; }

export default function DashboardGeometryCalibrationOverlay(_props: Props) {
  return null;
}
